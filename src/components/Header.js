import Logo from '../assets/images/Logo.svg';

const Header = () => {
    return (
        <header>
            <img alt="Little Lemon Logo" src={Logo}/>
        </header>
    );
}

export default Header;